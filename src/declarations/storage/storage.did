service : {
  inc: () -> (nat);
  get: (text) -> (text);
  store: (text, text) -> ();
  register: (text, text, text, text, text, text) -> ();
  searchObjectId: (text) -> (vec text);
  searchUserId: (text) -> (vec text);
}
